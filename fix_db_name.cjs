const admin = require('firebase-admin');

// No wait, I don't have the service account key easily here to run firebase-admin locally.
// I can just add a useEffect in App.tsx to forcefully update the store name if it is Avento7.
